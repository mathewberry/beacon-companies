package com.mathewberry.companies.persist;

public interface IPersistence 
{
	// Load a value
	String loadSomething(String companyName);
	
	// Save a value
	void saveSomething(String companyName, String username, float balance);
	
	// Notify implementations, that the plugin goes down
	// time to store any pending changes
	void shutdown();
}
